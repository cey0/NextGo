# .air.toml
# Config file for air
root = "."
tmp_dir = "tmp"                # Temporary directory
include_ext = ["go", "tmpl"]   # File extensions to watch
exclude_dir = ["assets", "vendor", "tmp"] # Directories to exclude
build = {
  cmd = "go build -o ./tmp/main ./cmd/main.go" # Build command
  bin = "./tmp/main"                          # Binary to run
  working_dir = "."                          # Working directory
}
watch = {
  interval = "200ms"                        # Watch interval
}
